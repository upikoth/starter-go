definitions:
  http.ResponseSuccess:
    properties:
      data: {}
      success:
        default: true
        type: boolean
    type: object
info:
  contact: {}
  title: Starter API
paths:
  /api/v1/health:
    get:
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/http.ResponseSuccess'
      summary: Проверка работоспособности сервера
swagger: "2.0"
